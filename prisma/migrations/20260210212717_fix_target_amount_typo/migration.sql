/*
  Warnings:

  - You are about to rename the column `tragetAmount` to `targetAmount` on the `Project_stage` table.

*/
-- AlterTable
ALTER TABLE "Project_stage" RENAME COLUMN "tragetAmount" TO "targetAmount";
